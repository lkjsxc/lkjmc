# Active work

## Governing objective

Campaign [`docs/campaigns/202608312120.md`](../campaigns/202608312120.md), **Rust-Owned Fresh
Supported-Container Installation and Operator Acceptance**, governs this checkout. Its immediate
predecessor [`202608311119.md`](../campaigns/202608311119.md) is complete only through source,
deterministic, PostgreSQL/process, retained-release, and independent-retrieval evidence. It did not
install or run a supported target. The earlier fixed-topology campaign is historical only for
recovery scenario semantics.

The active outcome is one UUID-bound Rust `lkjmc-ops host install` operation for a prepared systemd
container. It anchors a release, configuration, and immutable server assets; creates only a fresh
owned identity/root/database closure; publishes the exact service; and distinguishes accepted
replay, resumable work, and conflicts. Fresh supported-host, player, and production claims remain
separate external evidence boundaries.

## Checkout and governing inputs

- Repository `/home/coder/workspace/lkjmc` (`lkjsxc/lkjmc`), branch `main`, currently at
  `42d0e9319d2fe55754b24533c20056fd55f9d8d5`, one commit ahead of `origin/main`; remote is
  `https://github.com/lkjsxc/lkjmc`. There is one worktree and no submodules or nested repositories.
- `42d0e931` committed the supplied durable policy/campaign/initial reconciliation and deleted the
  withdrawn `lkjmc-discord` crate and direct release/build/test/current-doc consumers. The current
  independent inventory is three Rust binaries, three Java jars, and two declarative systemd files.
  Residual searches are clean outside historical campaign/ledger context.
- Supplied policy SHA-256: `96a3188351b086769f533d8de4d75bfcd596401a3fff98c4f627e0f625382299`
  (`AGENTS.md`). Governing campaign SHA-256:
  `c61f63db77c3f6817f63fa3cf94c18785db29db9e9b373715371618989d6fa2e`.
- Current uncommitted changes are limited to the expected first-install implementation and owner
  documents; no unrelated user changes are present. `git diff --check` passes.

## Revalidated predecessor evidence

- GitHub Verify run `33373201044` for `13602f277bcc109d891bc3726976a0323936e8b3` succeeded.
  Release artifact `9751489948` and its consumer receipt were independently downloaded before their
  recorded 2026-09-30 expiry. The predecessor archive SHA-256 is
  `f8331ffa07ee2f94f0cafe462b1094bc7fdd158dafd3e5a805863f7f91b1f700`; its manifest SHA-256 is
  `c332af627a4cb2fc60856cb3be981f78821dc75c3f9044303c1539e523c590cd`.
- This is **RELEASE ARTIFACT VERIFIED** and independent-retrieval evidence only. Installation,
  supported-host, operator, protocol-client, real-player, and production evidence for that release
  is **NOT RUN**.

## Implemented current slice

- `lkjmc-ops host install --input PATH --input-sha256 DIGEST` has one strict versioned Rust input
  owner and a secret-free durable journal. It preflights systemd, Java, PostgreSQL 14, tools,
  capacity, listener availability, identity/path/database conflicts, release/configuration/asset
  anchors, and topology-neutral typed configuration before lkjmc mutation.
- It creates the dedicated `lkjmc` identity and private roots, independently generated scoped
  secrets, UUID-marked local PostgreSQL role/database/migrations, exact release/assets/plugins,
  root-owned EULA policy/materialization, systemd unit/drop-in, fence and one-use permit. Acceptance
  rechecks complete persisted fleet equality, daemon/Velocity readiness, systemd cgroup state, and
  exact daemon executable/UID/start identity before clearing the fence.
- Exact accepted replay is read-only; resumable work verifies completed state instead of replacing
  it. Altered EULA policy/files or an existing non-exact release now fail closed rather than being
  rewritten. The daemon respects the configured runtime-asset root.
- The first full verifier exposed one direct stale release-identity count (`215` Cargo packages after
  the withdrawn crate had been removed); the independent inventory now expects `214` and its focused
  regression passes.
- Current documentation describes the command as implemented but explicitly **not yet
  fresh-supported-host accepted**.

## Current evidence and blockers

- **FORMATTED**, **STATICALLY CHECKED**, and **UNIT TESTED**: the pinned verifier ran `cargo fmt
  --all -- --check`, `cargo clippy --locked --offline -p lkjmc-ops -p lkjmc-daemon --all-targets --
  -D warnings`, and focused `lkjmc-ops`/`lkjmc-daemon` tests. The ops suite includes release,
  journal/fence, PostgreSQL backup/restore, EULA, process, and first-install contract tests.
- **GENERATED-CONTRACT VERIFIED**: pinned checks passed for contracts, configuration examples,
  network adoption, bootstrap/asset/command docs, and permissions.
- Native Rust/Java/Gradle/PostgreSQL/Incus/LXD executables are absent on this checkout host. Docker
  supplies the pinned verifier only; it is not supported-host evidence. Read-only discovery found no
  `incus` or `lxc` client and no active Incus/LXD service, so no authorized supported-container
  manager is currently available. Do not substitute Docker for that lane.
- The first full verifier stopped at that stale count before release construction; the count was
  corrected without changing the intended eight-member payload. Fresh final workspace/release
  verification, exact remote artifact retrieval, and formal external lane disposition are pending
  the next clean commit.

## Next executable action

Commit the release-identity correction, then rerun the clean full verifier and two-root release build
for that exact commit. Push and independently retrieve its workflow artifact. If no authorized
Incus/LXD manager becomes available, record supported-host, operator, protocol-client, restart, and
isolated-restore tiers as **BLOCKED** without mutating external targets.
