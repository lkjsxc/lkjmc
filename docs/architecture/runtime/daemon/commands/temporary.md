# Temporary commands

## Purpose

This generated file lists `temporary` daemon command literals from
[contracts/commands.json](../../../../../contracts/commands.json).

## Status

implemented

## Commands

| Command | Authorization | Surfaces | Summary |
| --- | --- | --- | --- |
| `temporary.instance.cleanup` | operator | cli, discord, paper, velocity, web | Execute temporary instance cleanup behavior for the temporary command family. |
| `temporary.instance.create` | operator | cli, discord, paper, velocity, web | Execute temporary instance create behavior for the temporary command family. |
| `temporary.instance.get` | operator | cli, discord, paper, velocity, web | Execute temporary instance get behavior for the temporary command family. |
| `temporary.instance.start` | operator | cli, discord, paper, velocity, web | Execute temporary instance start behavior for the temporary command family. |
| `temporary.instance.stop` | operator | cli, discord, paper, velocity, web | Execute temporary instance stop behavior for the temporary command family. |
| `temporary.transfer.intent` | operator | cli, discord, paper, velocity, web | Execute temporary transfer intent behavior for the temporary command family. |
