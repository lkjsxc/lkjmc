# Asset commands

## Purpose

This generated file lists `asset` daemon command literals from
[contracts/commands.json](../../../../../contracts/commands.json).

## Status

implemented

## Commands

| Command | Authorization | Surfaces | Summary |
| --- | --- | --- | --- |
| `asset.plugin.inspect` | operator | cli, discord, paper, velocity, web | Execute asset plugin inspect behavior for the asset command family. |
| `asset.plugin.list` | operator | cli, discord, paper, velocity, web | Execute asset plugin list behavior for the asset command family. |
| `asset.plugin.sync` | operator | cli, discord, paper, velocity, web | Execute asset plugin sync behavior for the asset command family. |
| `asset.server.sync` | operator | cli, discord, paper, velocity, web | Execute asset server sync behavior for the asset command family. |
