# Security commands

## Purpose

This generated file lists `security` daemon command literals from
[contracts/commands.json](../../../../../contracts/commands.json).

## Status

implemented

## Commands

| Command | Authorization | Surfaces | Summary |
| --- | --- | --- | --- |
| `security.daemon-token.plan` | admin | cli, discord, paper, velocity, web | daemon HTTP token rotation in security_api.rs. |
| `security.daemon-token.rotate` | admin | cli, discord, paper, velocity, web | daemon HTTP token rotation in security_api.rs. |
| `security.daemon-token.status` | admin | cli, discord, paper, velocity, web | daemon HTTP token rotation in security_api.rs. |
| `security.daemon-token.verify` | admin | cli, discord, paper, velocity, web | daemon HTTP token rotation in security_api.rs. |
