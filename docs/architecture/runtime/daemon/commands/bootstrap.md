# Bootstrap commands

## Purpose

This generated file lists `bootstrap` daemon command literals from
[contracts/commands.json](../../../../../contracts/commands.json).

## Status

implemented

## Commands

| Command | Authorization | Surfaces | Summary |
| --- | --- | --- | --- |
| `bootstrap.apply` | operator | cli, discord, paper, velocity, web | Execute bootstrap apply behavior for the bootstrap command family. |
| `bootstrap.doctor` | operator | cli, discord, paper, velocity, web | Execute bootstrap doctor behavior for the bootstrap command family. |
| `bootstrap.plan` | operator | cli, discord, paper, velocity, web | Execute bootstrap plan behavior for the bootstrap command family. |
| `bootstrap.status` | operator | cli, discord, paper, velocity, web | Execute bootstrap status behavior for the bootstrap command family. |
