# E-MENU local and candidate evidence run

## Purpose

Record E-MENU static, manifest-selected compiled-resource, daemon-candidate,
fault, and blocked-protocol observations without treating any result as
Minecraft player proof.

## Identity and provenance

- Hypothesis: [E-MENU](../experiments/e-menu-20260712.md).
- Capture input commit: `6ef63dc538880cc5709e8e7807f24f133f960c91`.
- Capture: seed `20260712`, one Linux run; Java 21, Gradle 8.10.2, Cargo,
  Docker, and disposable Compose PostgreSQL.
- Raw root: `/tmp/lkjmc-e-menu-final-20260712-tip`; replay passed at capture tip.
- Manifest SHA-256: `58dff40e0c70dc37281ed5ffc76cad17c2cffab03b023f419015d9f775e39fe0`.
- Manifest source tip: `758c3eb322e65eb215e21c0676822c111416bda8b243590c12c22ed6d7919384`.

The source manifest explicitly excludes this run and its decision record. Its
source tip hashes every selected source path and content, rather than the full
repository tip. Replay verifies raw hashes, the manifest, every selected source
hash, that the input commit exists, and the current manifest source tip. Thus
later excluded evidence prose cannot self-alter the measured specimen.

## Local and compiled observations

| Probe | Command or artifact | Result and boundary |
| --- | --- | --- |
| static catalog | `./scripts/check-menus.py`; generator `--check` | PASS. Four local documentation metadata records only; no rendering proof. |
| locale | `./scripts/check-locales.py` | PASS. English/Japanese structure only; no player language observation. |
| normalized route | common `DocCoreTest` | PASS. Local paths normalize and traversal/absolute paths reject. |
| local docs surface | Paper `LocalPaperSurfaceTest` | PASS. Resource, local metadata, and slot-8 token checked; no Bukkit event. |
| compiled bundle | Paper `shadowJar`; `compiled-resource.json` | PASS. Full resource had 269 paths. The manifest-selected compiled specimen had 267 paths and SHA-256 `ac9666e8015d2aa1fdc0e1522d02ae3d0fb3d177e580026a3ba9ff5122ba48f0`. |
| layout specimen | `compiled-local-docs-layout.json` | Root slots 0–44 contain selected documents; `docs/README.md` page zero used 19–28, 49, and 53. Renderer-derived only. |
| malformed bundle | isolated Java call to compiled `DocBundle` | PASS. Invalid JSON threw `com.google.gson.JsonSyntaxException`. |

The full resource's two extra paths are this run and decision. They are retained
as unmeasured bundle diagnostics in raw output. The manifest-selected count,
hash, and layout are the stable compiled evidence; none is a Bukkit/client
golden, inventory interaction, accessibility, consent, or player-session claim.

## Daemon candidates and outage

The daemon candidate command was:

```sh
docker compose --profile verify run --rm --build verify cargo test --locked \
  -p lkjmc-daemon menu_data_commands_return_documented_shapes_when_database_configured
```

It passed after a Compose rebuild. Raw `daemon-view-model-candidates.txt`
records the BuildKit build manifest, and `compose-image-provenance.txt` records
the rebuilt verify-image inspect result. One PostgreSQL-backed response-shape
test passed; its 17 read-view values have no Java consumer, menu binding,
identity, or inventory row.

A no-database daemon Unix-socket `instance list` attempt returned exit 1:
`database.not_configured: Database URL is not configured`. This is a daemon
outage boundary, not a menu screen or recovery observation.

## Protocol-player attempts

| Attempt | Exact command | Result |
| --- | --- | --- |
| Java menu/protocol player | `LKJMC_MINECRAFT_SMOKE=1 ./scripts/check-minecraft-smoke.sh` | exit 1, `BLOCKED`: trusted identity/session attestation remains absent. |
| playable protocol player | `LKJMC_PLAYABLE_SMOKE=1 LKJMC_ACCEPT_MINECRAFT_EULA=1 ./scripts/check-playable-smoke.sh` | exit 1, `BLOCKED`: no bounded local-safe Java protocol smoke exists. |

Neither blocked result is a pass, simulated player, or reason to restore a Java
daemon route.

## Rerun

```sh
ROOT="$(git -C "$REPO" rev-parse --show-toplevel)"
SCRIPT="$ROOT/docs/research/runs/e-menu-20260712.py"
(cd /tmp && python3 "$SCRIPT" capture)
(cd /tmp && python3 "$SCRIPT" replay --raw-dir /tmp/lkjmc-e-menu-final-20260712-tip)
```

Retain the owned raw root for review; do not treat replay as product adoption.
