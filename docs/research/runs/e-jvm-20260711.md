# E-JVM isolated adapter run

## Purpose

Record one reproducible, local Java 21 comparison without changing a plugin,
daemon, controller, runtime, or shipped synchronization claim.

## Identity and command evidence

- Hypothesis: [E-JVM isolated comparison](../experiments/e-jvm-20260711.md).
- Base: `d20e5e532db9d3a5577f567dd6a5a24fdc51eea1`.
- Harness result: `e15c264d15c3dab2d8f47ce68d6bc797e680e83c`; immutable
  source tip: `6fc8c2c059f07582f7b4dfb16508d0e562e6ce5e`.
- Environment: Linux, OpenJDK `21.0.11`, Gradle `8.10.2`; no credentials,
  sockets, database, Minecraft server, Velocity proxy, or daemon were used.
- Data: fixed revisions `1,2,3,4`, pushed IDs `2,4`, two failed reconnects,
  eight held jobs, and correlation ID ending `0011`.

The runner derived the source worktree from its own path. It was invoked from
`/tmp`; its retained, ignored raw root is `/tmp/lkjmc-e-jvm-6dNAPKG3`. It contains the
exact Gradle command, Java version, Gradle output, result, outcome, and
SHA-256 index. Replay rehashed all five retained files.

```sh
git worktree add --detach /tmp/lkjmc-e-jvm-evidence \
  6fc8c2c059f07582f7b4dfb16508d0e562e6ce5e
REPO=/tmp/lkjmc-e-jvm-evidence
ROOT="$(git -C "$REPO" rev-parse --show-toplevel)"
SCRIPT="$ROOT/docs/research/runs/e-jvm-20260711.sh"
(cd /tmp && "$SCRIPT" run)
(cd /tmp && "$SCRIPT" replay --raw-dir /tmp/lkjmc-e-jvm-6dNAPKG3)
(cd "$ROOT" && ./gradlew --no-daemon --no-build-cache test)
```

These commands require that source-only Gradle project and do not apply to this
evidence-only tree. The runner's nested Gradle execution returned `PASS`: three JUnit tests passed,
then its Java main wrote `result.json`. The root Gradle command also passed for
common, Paper, and Velocity modules. The result is local JDK evidence only.

## Candidate observations

| Area | Observation | Result |
| --- | --- | --- |
| adaptive polling | Pure revision snapshots reached head `4`; this is not a polling-rate measurement. | PASS |
| long-poll | Bounded waits returned contiguous revisions `2,3,4`. | PASS |
| SSE | Delivered IDs `2,4` differed from the complete feed, so it did not converge. | PASS, negative control |
| push repair | A durable read from last contiguous revision repaired to head `4` with one read. | PASS |
| reconnect limit | Two unavailable retries ended `OFFLINE`; no third retry or fresh claim occurred. | PASS |
| future fence | A JDK scheduler task submitted a held future and returned before that future completed. | PASS |
| worker comparison | Fixed pool started `2` held jobs before release; virtual workers started `8`. | PASS |
| typed dispatcher | Two research-local operations were accepted and one withdrawn effect was rejected. | PASS |
| Velocity topology | Static `edge` mapped to `lobby`; missing route was absent and discovery count was `0`. | PASS |
| workflow outcomes | All four auth/attestation combinations retained one correlation ID; three denied and one was `RESEARCH_ONLY`. | PASS |

The scheduler result proves only the harness's JDK executor did not wait for its
held worker. It does not prove a Paper, Folia, or Velocity scheduler path. The
virtual-worker count is a held-job concurrency observation, not capacity,
latency, or a daemon executor choice. The transport rows use no Java networking
and do not establish SSE, HTTP, push delivery, authentication, or recovery.

## External lanes and cleanup

| Lane | Outcome | Reason and reconsideration |
| --- | --- | --- |
| Java daemon transport | BLOCKED, not attempted | Current withdrawal prohibits a Java daemon client or mock endpoint. Trusted identity/session attestation and a new owner contract are required. |
| Minecraft scheduler | BLOCKED, not attempted | A plain JDK executor is not a server scheduler. A separately approved disposable platform harness is required. |
| Velocity topology and transfer | BLOCKED, not attempted | Dynamic registration and transfer are withdrawn; static lookup is not proxy evidence. |
| authenticated workflow | BLOCKED, not attempted | No credential or session was created. `RESEARCH_ONLY` is not authorization or player arrival. |

No external lane is reported as passed or available. The runner owns only its
temporary directory; `cleanup --raw-dir /tmp/lkjmc-e-jvm-6dNAPKG3` removes it
when independent replay is complete. Interpretation remains in the
[decision](../decisions/e-jvm-20260711.md).
