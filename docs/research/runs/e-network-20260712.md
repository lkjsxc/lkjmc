# E-NETWORK bounded compiler run

## Purpose

Record the isolated E-NETWORK observation without adopting a network controller,
product runtime, command, asset, plugin, or Kubernetes object.

## Identity

- Base: `4b9357a8e1a7949e0ebfe59c16af5196554f46cc`.
- Hypothesis: `7939d0c`; harness: `9d10f25`.
- Document SHA-256: `73d218255bd5553ee4c461edbbc6294cc399b5f34fed31589e7636953a3b7ade`.
- Raw root: `/tmp/lkjmc-e-network-o_9toqww`; index SHA-256:
  `b81a4fc3cf7f09dddf9b2136a1616adb4cc982000773722aad812d51bf43a592`.
- Input is the committed research JSON document; no network download, secret,
  Kubernetes apply, product configuration, or controller state was used.

## Commands

```sh
ROOT="$(git rev-parse --show-toplevel)"
SCRIPT="$ROOT/docs/research/runs/e-network-20260712.py"
(cd /tmp && PYTHONDONTWRITEBYTECODE=1 python3 "$SCRIPT" run)
(cd /tmp && PYTHONDONTWRITEBYTECODE=1 python3 "$SCRIPT" replay \
  --raw-dir /tmp/lkjmc-e-network-o_9toqww)
cargo test -p lkjmc-core bootstrap::tests
```

The harness returned zero and replay returned `E-NETWORK replay=PASS`. The
existing bootstrap test selector passed eight tests. It validates current pure
bootstrap behavior, not the research compiler or a live network.

## Observations

| Probe | Observation | Outcome |
| --- | --- | --- |
| inspect/apply | Empty inspection became proxy and hub specifications after two real local children started. | PASS |
| reapply | The second inspect/apply started no child; its event record contains only the original two starts. | PASS |
| change | Enabling optional `events` added a third observed child. | PASS |
| rollback | Reconcile to the base document stopped `events`; proxy and hub remained observed before root cleanup. | PASS |
| removal | Removing `events` stopped it and restarted proxy because proxy routing changed; hub stayed observed. | PASS |
| controlled failure | `events` started, then a deliberately injected post-start error stopped it; base proxy and hub remained observed. | PASS |
| unsupported policy | The pure compiler rejected Kubernetes after policy was restricted to `local-process`. | PASS |
| Kubernetes manifests | The pure adapter rendered six objects: ConfigMap, Deployment, and Service for proxy and hub. | PASS |
| imperative model | Two blind passes started two children each, leaving four live before cleanup. | PASS, research model only |
| process cleanup | All 34 recorded child PIDs were absent after cleanup. | PASS |

The imperative row is not an observation about the daemon's `bootstrap.apply`.
It is the bounded comparison model described by the hypothesis. The local
children are not Minecraft, and document digest strings are not fetched assets.

## Kubernetes capability attempt

The exact attempted command was `kubectl version --client --output=yaml`. It
returned exit `127` with `[Errno 2] No such file or directory: 'kubectl'`.
Therefore the capability outcome is `BLOCKED`, not Kubernetes success. The
harness did not apply, inspect, or claim cluster objects. A rerun additionally
requires `LKJMC_KUBERNETES_SMOKE=1` and `LKJMC_KUBERNETES_NAMESPACE` naming an
authorized disposable namespace after the client succeeds.

## Limits and retention

The local adapter has no ports, Minecraft protocol, durable intent, real asset
verification, plugin installation, daemon restart, or cluster observation. Its
rollback covers only a child started by this harness; it cannot undo an external
effect. Keep the ignored raw root for review, then run the harness `cleanup`
action with that exact path. The interpretation remains research-only in the
[decision](../decisions/e-network-20260712.md).
