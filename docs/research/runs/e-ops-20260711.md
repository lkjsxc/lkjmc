# E-OPS reproducibility run 2026-07-11

## Purpose

Record bounded operations evidence without claiming a product deployment,
rootless runtime, cluster, signature, or support bundle.

## Identity and environment

- Experiment: [E-OPS hypothesis](../experiments/e-ops-20260711.md).
- Product base: `d20e5e532db9d3a5577f567dd6a5a24fdc51eea1`.
- Harness commit: `3446580f2b5634a03328697c5486b06c2b4d7e73`.
- Seed: `20260711`; repetitions: one correctness run per lane.
- Raw redacted artifacts: `/tmp/lkjmc-e-ops-57q5u5av`.
- Artifact index SHA-256: `da5ed4b7d356f4cfb6f23b1dd9857b5f532af4826eb051d86f548bd8ac28921b`.

Docker Server was `29.5.3`. The verified container reported Rust/Cargo `1.97.0`,
Gradle `8.10.2`, Temurin Java `21.0.5`, and Python `3.10.12`. No capacity or
player workload was used.

## Commands and results

The runner was invoked from the isolated worktree:

```sh
python3 -B docs/research/runs/e-ops-20260711.py
```

| Probe | Exact attempted command and exit | Result |
| --- | --- | --- |
| `clean-compose-run` | `docker compose --project-name lkjmceops7833fddf34 -f /tmp/pi-agent-092dc5da-d45d-44b-d67f0b35/docker-compose.yml --profile verify build --no-cache verify` (0, 79.918s); then `... --profile verify run --rm verify` (0, 126.425s) | PASS |
| `nested-skip-evidence` | Same Compose verifier command (0) | PASS; exact final `ran` and `skipped` fields are retained below. |
| `rootless-attempt` | `docker info --format '{{json .SecurityOptions}}'` (0, no `rootless` option); `docker --context rootless info --format '{{json .SecurityOptions}}'` (1) | EXTERNAL PENDING: context `rootless` does not exist. No Compose config-only result was accepted as rootless proof. |
| `restore-start-run` | Unique-project `up -d postgres`, `pg_isready`, CLI `db migrate`, `pg_dump --format=custom`, `createdb`, `pg_restore --clean --if-exists`, daemon `doctor`, and `dropdb` (all 0) | PASS |
| `kube-lab-attempt` | `kubectl version --client --output=yaml` (127); `LKJMC_KUBERNETES_SMOKE=1 ./scripts/check-kubernetes-smoke.sh` (2) | EXTERNAL PENDING: `kubectl` is absent and the guarded script stopped at unset `LKJMC_KUBERNETES_CONFIG`. |
| `systemd-sandbox-run` | `systemd-run --user --wait --pipe --collect -p NoNewPrivileges=yes -p PrivateTmp=yes -p ProtectSystem=strict /bin/sh -ec 'test ! -w /usr; test -w /tmp; id -u'` (0) | PASS for the disposable user-manager scope; it ran as UID 1001. |
| `artifact-provenance` | `sha256sum Cargo.lock Dockerfile docker-compose.yml gradle/wrapper/gradle-wrapper.properties`; compact `cargo metadata --locked --no-deps`; two isolated release daemon builds (all 0) | PASS; both daemon hashes were `e64ff8e0e143e2cc7fdaccba7b2b9847b4fac3d71719dc5cbf761b0a90e5ed74`. |
| `toolchain-acquisition-verified` | Container version command (0) plus Dockerfile/wrapper audit | FAIL: no base-image digest, Rustup checksum, or Gradle wrapper distribution checksum is committed. |
| optional signing | `git verify-commit d20e5e532db9d3a5577f567dd6a5a24fdc51eea1` (1) | EXTERNAL PENDING: no trusted signature proof was established. |
| fault evidence | `docker compose ... run --rm --no-deps verify python3 scripts/check-fault-harness.py --all` (0, 29.082s) | PASS for test-only boundaries and release-marker inspection. |

The exact verifier summary was:

```text
ok verify-full ran=line-guard-boundary,playable-default-secure,full-skip-summary-truthful,deterministic-smokes-run,real-config-parser,atomic-download-faults,partial-final-files-zero,migration-lock-checksum,database-deadlines,jar-registry,claim,web skipped=docker-secret-context-clean:docker,jar-live:LKJMC_JAR_LIVE_SMOKE,installer:LKJMC_INSTALLER_SMOKE,plugin-assets:LKJMC_PLUGIN_LIVE_SMOKE
```

The component inventory was `lkjmc-core`, `lkjmc-store`, `lkjmc-daemon`,
`lkjmc-cli`, `lkjmc-discord`, and `lkjmc-xtask`. The input hashes are retained
in the raw index; no credential or dump contents were retained.

## Restore, faults, and cleanup

The restore database was uniquely named, dumped from Compose PostgreSQL,
restored, used by a newly compiled daemon, queried with `doctor`, explicitly
dropped, then removed with its Compose volume. The daemon command exited 0 and
printed `ok doctor`. This proves only the isolated database boot path.

The fault harness passed failpoint, transaction, effect, deadline,
cross-instance, and seeded-replay checks. It is not representative-load proof,
and no support-bundle implementation exists to inspect. Do not describe it as
support-artifact, player, or release evidence.

`docker compose ... down --volumes --remove-orphans` exited 0. The uniquely
named verify images were then removed with `docker image rm -f`; no matching
container or network remained. Raw evidence remains ignored under the listed
`/tmp` directory for independent review.

## Deviations and boundary

The systemd result is a user-manager scope, not a production unit or a
containerized PID 1 result. Rootless Docker, Kubernetes, and signing were
attempted but are pending external prerequisites. The failed immutable-toolchain
audit is a local finding, not a passing acquisition claim. No owner contract,
state row, controller state, or product source changed.
