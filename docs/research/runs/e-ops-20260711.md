# E-OPS reproducibility run 2026-07-11

## Purpose

Record bounded operations evidence without claiming a product deployment,
rootless runtime, cluster, signature, or support bundle.

## Identity and environment

- Experiment: [E-OPS hypothesis](../experiments/e-ops-20260711.md).
- Product base: `d20e5e532db9d3a5577f567dd6a5a24fdc51eea1`.
- Harness commit: `4a1a018`; replay began with a clean worktree.
- Seed: `20260711`; repetitions: one correctness run per lane.
- Raw redacted artifacts: `/tmp/lkjmc-e-ops-b768sa16`.
- Artifact index SHA-256: `b4e24272a0265585a75540c52e8ddd22e4cc79e529a1884c96d71754963bc82e`.

Docker Server was `29.5.3`. The verified container reported Rust/Cargo `1.97.0`,
Gradle `8.10.2`, Temurin Java `21.0.5`, and Python `3.10.12`. No capacity or
player workload was used.

## Commands and results

The runner was invoked from the isolated worktree:

```sh
python3 -B docs/research/runs/e-ops-20260711.py
```

| Probe | Exact attempted command and exit | Result |
| --- | --- | --- |
| credential canary | `sh -ec 'printf ... postgresql URL ...'` (0); retained-artifact scan | PASS: the command header and output contain `<redacted-url>`; the canary is absent. |
| `clean-compose-run` | Unique-project `docker compose ... build --no-cache verify` (0); then `... run --rm verify` (1) | FAIL: `failed_server_download_is_durably_audited_when_database_is_configured` hit PostgreSQL lock timeout. |
| `nested-skip-evidence` | Same failed Compose verifier command (1) | FAIL: no final exact `ran`/`skipped` summary was retained. |
| `rootless-attempt` | `docker info --format '{{json .SecurityOptions}}'` (0, no `rootless`); `docker --context rootless info ...` (1) | EXTERNAL PENDING: the rootless context does not exist; no config-only result was accepted. |
| `restore-start-run` | Unique-project `up -d postgres`, readiness, migrate, dump, restore, daemon `doctor`, and dropdb (all 0) | PASS: isolated restore and daemon boot completed. |
| `kube-lab-attempt` | `kubectl version --client --output=yaml` (127); guarded Kubernetes smoke (2) | EXTERNAL PENDING: `kubectl` is absent and `LKJMC_KUBERNETES_CONFIG` is unset. |
| `systemd-sandbox-run` | Disposable `systemd-run --user ...` (0) | PASS for a user-manager scope only; UID was 1001. |
| `artifact-provenance` | Manifest hashes, component inventory, and two release builds (all 0) | PASS: both daemon hashes were `e64ff8e0e143e2cc7fdaccba7b2b9847b4fac3d71719dc5cbf761b0a90e5ed74`. |
| `toolchain-acquisition-verified` | Container version command (0) plus Dockerfile/wrapper audit | FAIL: no base-image digest, Rustup checksum, or Gradle distribution checksum is committed. |
| optional signing | `git verify-commit d20e5e532db9d3a5577f567dd6a5a24fdc51eea1` (1) | EXTERNAL PENDING: no trusted signature proof was established. |
| fault evidence | Test-only fault harness (0) | PASS for test-only boundaries and release-marker inspection. |
| `unique-image-cleanup` | Compose `down --volumes --remove-orphans` (0); `docker image rm -f lkjmceopsad678129a9-verify` (0); `docker image inspect ...` (1) | PASS: the image was absent after removal. |

## Restore, faults, and cleanup

The restore database was uniquely named, dumped from Compose PostgreSQL,
restored, used by a newly compiled daemon, queried with `doctor`, explicitly
dropped, then removed with its Compose volume. This proves only the isolated
database boot path.

The harness sanitizes command headers, URLs, and command output before every
artifact write. Its credential-canary scan passed; raw evidence contains neither
the canary nor a database URL with credentials. The image-absence probe is
retained with the cleanup logs. Raw evidence remains ignored under the listed
`/tmp` directory for independent review.

## Deviations and boundary

The clean Compose verifier failed at a PostgreSQL lock timeout, so it is not
clean-gate or nested-skip proof. Rootless Docker, Kubernetes, and signing were
attempted but remain pending external prerequisites. The immutable-toolchain
audit failed and is not a passing acquisition claim. The systemd result is not
a production unit or containerized PID 1 result. No owner contract, state row,
controller state, or product source changed.
