# E-DATA correction run 2026-07-11

## Purpose

Record the corrected disposable evidence without adopting a schema, controller,
workflow, Java adapter, delivery mechanism, or player-facing behavior.

## Identity and artifacts

- Experiment: [E-DATA hypothesis](../experiments/e-data-20260711.md).
- Worktree head: `7d0cd097248835fbb9d03621c044e3aa6414a1f9` with the corrected
  harness source hashes below; seed: `20260711`.
- Command: `python3 "$REPO/docs/research/experiments/e-data-20260711.py" --output /tmp/lkjmc-e-data-correction`.
- Result: exit `0`; raw root: `/tmp/lkjmc-e-data-correction`; result SHA-256:
  `288b90ebb3268bfc63a9f90bd0892c47f2d3ca1e2a712fb4c13f2eaf1cd84a86`.
- Main harness SHA-256:
  `623debc1c2f49d9ae34bd46d5fba8ca7b47ea6c71584e7a06542b140afe3f921`;
  support SHA-256:
  `7a2fad588cd79e37daa74b4936233abe38ff8a3a1dfcc30d55d10aaad63b4613`;
  candidate SQL SHA-256:
  `7a4f80e21c1e0d486343ab7812561dca0d09482f4dca80c3e64f8d3c3672d798`.

The harness built locked CLI and daemon binaries, used a new Compose PostgreSQL
container, and removed its containers and volumes. Raw logs are capped and
retained only in the ignored output root.

## Migration lock record

The rejected candidate's three-attempt loop retained no failing raw log, so it
cannot truthfully establish the old timeout's exact bytes or cause. This run
therefore records a controlled preceding Compose/DB migration attempt instead
of claiming that an unrecorded retry resolved anything.

| Attempt | Exit | Log SHA-256 | Cause classification |
| --- | ---: | --- | --- |
| `migrate-lock-timeout` | 1 | `350ba2648ffa11b31be174e8f2911621f540d37fa218daf29f02f853d2506aac` | expected, `pg_advisory_lock(7526470)` held for six seconds |
| `migration-lock-holder` | 0 | `3c5b6d06a51d7a558e2a24d43dc4086568f3282df52b232c801f992ea344c4d6` | controlled cause completed |
| `migrate-after-lock-release` | 0 | `37d3a80fe557e616cf8103725aa759d0ee29f2cc7b50cd61770bf17d60d268e1` | post-conflict setup continuation, not resolution proof |

`migrate-lock-timeout.log` is verbatim:

```text
postgres error: canceling statement due to lock timeout
```

The later success only shows that a new migration command ran after the
intentional holder released the lock. It does not diagnose the earlier
unretained incident or prove a retry policy is safe.

## Representative and fault results

| Probe | Result | Observation |
| --- | --- | --- |
| current profile/transfer slice | PASS | Real daemon plus CLI wrote and restored two current profile revisions; real UNIX `player.transfer.saved` wrote its audit row. |
| database delay | PASS | A six-second `ACCESS EXCLUSIVE` profile-table lock made the current CLI snapshot exit `1`; its log was `instance.error: postgres error: canceling statement due to lock timeout`; snapshot count stayed two. No retry was run. |
| typed profile integrity | PASS | Private typed JSON rejected malformed shape and checksum while opaque bytes admitted arbitrary data. |
| delivery variants | PASS | Weak rows admitted duplicate correlation; a private fence rejected stale acknowledgement after reclaim. |
| rollback, compensation, revision feed | PASS | Candidate rollback removed its row; points balance equaled ledger; private revision history held one immutable event. |
| private cutover restore | PASS | A full disposable archive included public and the random private schema, then reset, explicit public recreation/private drop, restore, migration status, and candidate checks passed. |
| workflow/journal/plugin acknowledgement | BLOCKED | No current path joins revision, transfer journal correlation, and a real plugin acknowledgement. |

The full archive SHA-256 is
`2f4ae518a4705f4a9cee1a7319e51dc8c475f393d69c9ddb3a57caadcfec4fa6`.
`dump-private-cutover`, `restore-private-cutover`, and `private-cutover-check`
exited `0`; their log hashes are respectively
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`,
`e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`, and
`54e40b5bbcd5017a42d5947e32ff44f0df8cf64d916fd625f131e25676469a94`.

## Boundary and cleanup

`player.transfer.saved` is an audit acknowledgement, not a transfer or item
receipt. The direct command used a CLI actor, not a fabricated plugin. The
current source has no wired transfer journal and no real Paper/Folia or Velocity
adapter; the blocked combination remains unproved. No product migration, store
helper, controller file, or Java source changed. `compose-down` exited `0`.

See the [decision](../decisions/e-data-20260711.md) for the non-adoption
outcome and the next required evidence.
