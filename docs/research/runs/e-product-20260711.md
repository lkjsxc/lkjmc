# E-PRODUCT bounded journey run 2026-07-11

## Purpose

Record reproducible local-model, PostgreSQL-model, and guarded-unavailable
journey evidence without claiming a Minecraft player, menu, protocol, transfer,
or gameplay result.

## Identity and environment

- Experiment: [E-PRODUCT hypothesis](../experiments/e-product-20260711.md).
- Product base: `af40e5569d4bc48c71eba1d2524d0b4f4a0f11e4`.
- Runner commit: `c261f4602b4e38d818119c649627f5717ee3b7f8`.
- Seed: `20260711`; one baseline and one candidate repetition.
- Toolchain: Python 3.12.3, Cargo 1.96.0, Java 21.0.11, Docker 29.5.3.
- Redacted local artifacts: `/tmp/lkjmc-e-product-0zl9b7n9` and
  `/tmp/lkjmc-e-product-f_ho4imp`; both index replays passed.

The baseline is the current broad bundled documentation browser. The candidate
is only a six-document curation selection evaluated from that same bundle; it
has no route, registration, player data, or product-source change.

## Baseline and candidate evidence

Both runs used `python3 docs/research/runs/e-product-20260711.py capture` with
`--variant baseline` or `--variant candidate`. Each returned zero. The bundle
had 238 documents, contained the six selected documents and the operator
quickstart, and reported `runtimeRoute: absent`. Therefore curation membership
is a documentation-model result, not a first-player completion result.

| Probe | Baseline and candidate observation | Result and limit |
| --- | --- | --- |
| `operator-baseline-candidate` | `cargo run -p lkjmc-cli -- --help` printed CLI usage (its help exit is 1); guarded `LKJMC_WEB_SMOKE=1 ./scripts/check-web-smoke.sh` returned 0. | Local CLI and authenticated-web checks only; no new operator journey was added. |
| `player-baseline-candidate` | Broad local bundle and containment check both succeeded. | `BLOCKED` for a player journey: no Minecraft client or inventory interaction ran. |
| `curated-player-docs` | All six selected paths are in the actual bundle. `player-help.md` still says its Help route is planned. | Research model passed; curated route is not adopted. |
| `locale-accessibility` | `./scripts/check-locales.py` returned 0; English/Japanese keys and JVM references are structurally valid. | No native-language or client accessibility observation exists. |

The initial capture at `/tmp/lkjmc-e-product-kt3nxz12` classified CLI help and a
daemon test incorrectly, then retained the raw results. Runner commit `b270205`
fixed those expectations without changing product code. The successful variant
runs above are the comparison evidence.

## Durable-model combinations

The runner passed `cargo test -p lkjmc-core adventure::tests` (two tests) and
`cargo test -p lkjmc-daemon player_shop_tests` (five tests). The former keeps
only `end-expedition` and rejects generic labels. The latter rejects retired or
caller-shaped adventure delivery metadata and requires confirmation before a
database connection. Neither test starts an adventure for a player.

A real disposable PostgreSQL command also returned zero:

```sh
docker compose --profile verify run --rm verify cargo test -p lkjmc-store \
  --test wake_join --test temporary --test safe_economy \
  --test adventure_shop_delivery_migration
```

It passed 3 adventure-delivery migration tests, 3 safe-economy tests, one
temporary-session test, and one wake-queue test. The queue reached durable
`queued`, `starting`, and `ready`; temporary and settlement records round
tripped; invalid delivery did not debit points; replay kept the settled item.
This is the `wake-transfer-combination`, `progression-exploit-suite`, and
`adventure-variants-run` evidence. It does not prove a wake display, transfer,
arrival, inventory mutation, world asset, datapack objective, or exploit-free
player economy.

## Exact unavailable attempts

The runner set each guard rather than accepting a skip:

| Surface | Exact command | Exit and result |
| --- | --- | --- |
| menu, chat fallback, protocol | `LKJMC_MINECRAFT_SMOKE=1 ./scripts/check-minecraft-smoke.sh` | 1, `BLOCKED`: Java daemon adapter smoke is withdrawn. |
| claim visualization/protection | `LKJMC_MINECRAFT_CLAIM_SMOKE=1 ./scripts/check-minecraft-claim-smoke.sh` | 1, `BLOCKED`: Java claim adapter is withdrawn. |
| playable adventure, wake, transfer | `LKJMC_PLAYABLE_SMOKE=1 LKJMC_ACCEPT_MINECRAFT_EULA=1 ./scripts/check-playable-smoke.sh` | 1, `BLOCKED`: no bounded local-safe Java protocol smoke exists. |

These outcomes satisfy neither a player journey nor a playable Compose claim.
They are deliberate F-SAFETY containment results, not failures to bypass.

## Gates and cleanup

`./scripts/verify-full.sh` passed, with unsupported external lanes reported as
skipped. `docker compose --profile verify run --rm verify` passed; the targeted
PostgreSQL command above also passed. Each Compose invocation was followed by
`docker compose --profile verify down --volumes --remove-orphans` with exit 0.

No candidate changed a product, adopter, controller, owner, or state document.
The raw roots are retained for review; rerun their printed replay command before
removing them. Rerun local variants from `/tmp` with:

```sh
ROOT="$(git -C "$REPO" rev-parse --show-toplevel)"
SCRIPT="$ROOT/docs/research/runs/e-product-20260711.py"
(cd /tmp && python3 "$SCRIPT" capture --variant baseline)
(cd /tmp && python3 "$SCRIPT" capture --variant candidate)
```
