# Archived data catalog

## Purpose

Committed provenance for the supplied `experiments/catalog/data.md` ID set.

## Imported candidates

- `DW-TRANSFER-FLOW`; `DW-PROFILE-FORMAT`; `DW-DELIVERY-FLOW`; `DW-ADVENTURE-FLOW`.
- `DW-RUNTIME-FLOW`; `DW-REVISION`; `DW-NOTIFY`; `DW-CHANGELOG`; `DW-DELTA`.
- `DW-FENCE`; `DW-LEDGER-INVARIANT`; `DW-AUDIT-INTEGRITY`; `DW-RETENTION`.
- `DW-CANONICAL-SCHEMA`; `DW-SNAPSHOT-BACKUP`; `DW-POOL-FAIRNESS`; `DW-CLOCK`.

## Boundary

These candidates do not alter PostgreSQL truth or workflow behavior. See
[research ideas](../ideas/data-workflows.md) for their current research boundary.
