# Archived control-plane catalog

## Purpose

Committed provenance for the supplied `experiments/catalog/control-plane.md` IDs.

## Imported candidates

- `CP-EXEC-ASYNC`; `CP-EXEC-BOUND`; `CP-EXEC-MIXED`; `CP-OPS-JOURNAL`.
- `CP-IDEMPOTENCY`; `CP-DEADLINE`; `CP-RUNTIME-KEYED`; `CP-RUNTIME-ACTOR`.
- `CP-RUNTIME-DBLOCK`; `CP-RECONCILE-EVENT`; `CP-NETWORK-SPEC`; `CP-CAPABILITY`.
- `CP-DOMAIN-SPLIT`; `CP-PROCESS-SUPERVISE`; `CP-SHUTDOWN`; `CP-CONFIG-APPLY`.
- `CP-HA-LEASE`.

## Boundary

These are research candidates, not daemon or orchestration behavior. See
[research ideas](../ideas/control-plane.md) for their current research boundary.
