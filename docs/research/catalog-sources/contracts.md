# Archived contract catalog

## Purpose

Committed provenance for the supplied `experiments/catalog/contracts.md` ID set.

## Imported candidates

- `CT-DOMAIN-SHARDS`; `CT-HAND-SCHEMA`; `CT-RUST-SCHEMA`; `CT-DOMAIN-SOURCE`.
- `CT-CHECKED-ADAPTER`; `CT-ERROR-TAXONOMY`; `CT-EFFECT-CLASS`; `CT-IDENTITY-POLICY`.
- `CT-CONFIG-DIR`; `CT-NETWORK-DOC`; `CT-LOCALE-SHARDS`; `CT-MENU-COMPILE`.
- `CT-SOURCE-PATHS`; `CT-CATALOG-INDEX`; `CT-HANDLER-COVERAGE`; `CT-MENU-SCHEMA`.
- `CT-WIDTH-CHECK`; `CT-REMOVE-ALIASES`.

## Boundary

These IDs are candidate identifiers, not shipped commands, schemas, or support
claims. Their current interpretation is in [research ideas](../ideas/contracts.md).
