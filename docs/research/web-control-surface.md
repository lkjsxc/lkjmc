# Web control surface

## Purpose

This document defines the future web UI seam.

## Direction

A future web UI should call the same daemon API used by the CLI and plugins. It
must use stable JSON request and response shapes, audit mutating actions, and
avoid exposing daemon control publicly by default.

## Current status

No web UI code is implemented or registered.
