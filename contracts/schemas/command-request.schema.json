{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Daemon command request body",
  "type": "object",
  "description": "Command-specific request bodies are JSON objects validated by handlers while strict per-command schemas are introduced."
}
