{
  "commands": [
    {
      "authorization": "operator",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/player.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::player_moderation::mute",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "player.moderation.mute",
      "request": {
        "fields": {
          "actorName": {
            "required": true,
            "type": "string"
          },
          "playerName": {
            "required": true,
            "type": "string"
          },
          "playerUuid": {
            "required": true,
            "type": "string"
          },
          "reason": {
            "required": true,
            "type": "string"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute player moderation mute behavior for the player command family.",
      "surfaces": [
        "cli"
      ]
    },
    {
      "authorization": "operator",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/player.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::player_moderation::status",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "player.moderation.status",
      "request": {
        "fields": {
          "playerName": {
            "required": false,
            "type": "string"
          },
          "playerUuid": {
            "required": true,
            "type": "string"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute player moderation status behavior for the player command family.",
      "surfaces": [
        "cli"
      ]
    }
  ],
  "domain": "player"
}
