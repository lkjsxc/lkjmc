{
  "commands": [
    {
      "authorization": "admin",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/shop.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::player_shop::upsert_item",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "shop.item.upsert",
      "request": {
        "fields": {
          "itemId": {
            "required": true,
            "type": "string"
          },
          "metadata": {
            "required": false,
            "type": "shop-metadata"
          },
          "pricePoints": {
            "required": true,
            "type": "integer"
          },
          "titleKey": {
            "required": true,
            "type": "string"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute shop item upsert behavior for the shop command family.",
      "surfaces": [
        "cli"
      ]
    }
  ],
  "domain": "shop"
}
