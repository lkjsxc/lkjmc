{
  "commands": [
    {
      "authorization": "operator",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/temporary.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::temporary_api::handle",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "temporary.instance.cleanup",
      "request": {
        "fields": {
          "force": {
            "required": false,
            "type": "boolean"
          },
          "id": {
            "required": true,
            "type": "string"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute temporary instance cleanup behavior for the temporary command family.",
      "surfaces": [
        "internal"
      ]
    }
  ],
  "domain": "temporary"
}
