{
  "commands": [
    {
      "authorization": "operator",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/proxy.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::proxy_registration::report",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "proxy.registration.report",
      "request": {
        "fields": {
          "proxyId": {
            "required": false,
            "type": "string"
          },
          "registrations": {
            "required": true,
            "type": "array"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Internal withdrawn Java adapter hook; no public surface is registered.",
      "surfaces": [
        "internal"
      ]
    }
  ],
  "domain": "proxy"
}
