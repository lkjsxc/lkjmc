{
  "commands": [
    {
      "authorization": "admin",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/config.md",
      "effect": "restart-required",
      "errors": "handler-defined",
      "handler": "crate::commands::config_api::reload",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "config.reload",
      "request": {
        "body": "handler-defined"
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Reject config reload because a daemon restart is required.",
      "surfaces": [
        "cli"
      ]
    }
  ],
  "domain": "config"
}
