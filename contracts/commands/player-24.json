{
  "commands": [
    {
      "authorization": "operator",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/player.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::player_report::resolve",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "player.report.resolve",
      "request": {
        "fields": {
          "reportId": {
            "required": true,
            "type": "string"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute player report resolve behavior for the player command family.",
      "surfaces": [
        "internal"
      ]
    },
    {
      "authorization": "operator",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/player.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::player_restore::restore",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "player.restore",
      "request": {
        "fields": {
          "playerUuid": {
            "required": true,
            "type": "string"
          },
          "scope": {
            "required": false,
            "type": "string"
          },
          "snapshotId": {
            "required": true,
            "type": "string"
          },
          "sourceInstance": {
            "required": false,
            "type": "string"
          }
        }
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute player restore behavior for the player command family.",
      "surfaces": [
        "cli"
      ]
    }
  ],
  "domain": "player"
}
