{
  "commands": [
    {
      "authorization": "admin",
      "deadline": "not-run",
      "doc": "docs/architecture/runtime/daemon/commands/economy.md",
      "effect": "denied-unproved",
      "errors": "handler-defined",
      "handler": "crate::commands::player_exchange::seed_defaults",
      "idempotency": "not-run",
      "identity": "transport-subject",
      "name": "economy.catalog.seed-defaults",
      "request": {
        "body": "handler-defined"
      },
      "response": {
        "body": "handler-defined",
        "envelope": "command-response-v1"
      },
      "summary": "Execute economy catalog seed-defaults behavior for the economy command family.",
      "surfaces": [
        "cli"
      ]
    }
  ],
  "domain": "economy"
}
